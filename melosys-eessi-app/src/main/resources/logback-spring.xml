<configuration>
  <springProfile name="!local || !local-mock">
    <include resource="logback-nais.xml"/>
  </springProfile>
  <springProfile name="local || local-mock">
    <include resource="logback-local.xml"/>
  </springProfile>
</configuration>
