<configuration>
  <springProfile name="nais">
    <include resource="logback-nais.xml"/>
  </springProfile>
  <springProfile name="test,local,local-mock">
    <include resource="logback-local.xml"/>
  </springProfile>
</configuration>
