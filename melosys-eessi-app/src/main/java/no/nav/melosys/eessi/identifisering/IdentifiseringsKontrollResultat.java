// Generated by delombok at Thu Jul 04 12:27:09 CEST 2024
package no.nav.melosys.eessi.identifisering;

import java.util.Collection;
import java.util.stream.Collectors;

public final class IdentifiseringsKontrollResultat {
    private static final String FEIL_BESKRIVELSE = "-- Automatisk SED-kontroll --\n" + "Feil i opplysninger av identifisert person: %s\n";
    private static final String OVERSTYRINGSMELDING = "Du kan overføre oppgaven hvis du ser en feil i mottatt SED. \nNB! Dette kan ikke omgjøres. Vennligst kontroller opplysningene før du overfører oppgaven.";
    private final Collection<IdentifiseringsKontrollBegrunnelse> begrunnelser;

    public boolean erIdentifisert() {
        return begrunnelser.isEmpty();
    }

    public String hentFeilIOpplysningerTekst() {
        String manglerTekst = begrunnelser.stream().map(IdentifiseringsKontrollBegrunnelse::getBegrunnelse).collect(Collectors.joining(", "));
        return String.format(FEIL_BESKRIVELSE, manglerTekst) + OVERSTYRINGSMELDING;
    }

    @java.lang.SuppressWarnings("all")
    public IdentifiseringsKontrollResultat(final Collection<IdentifiseringsKontrollBegrunnelse> begrunnelser) {
        this.begrunnelser = begrunnelser;
    }

    @java.lang.SuppressWarnings("all")
    public Collection<IdentifiseringsKontrollBegrunnelse> getBegrunnelser() {
        return this.begrunnelser;
    }

    @java.lang.Override
    @java.lang.SuppressWarnings("all")
    public boolean equals(final java.lang.Object o) {
        if (o == this) return true;
        if (!(o instanceof IdentifiseringsKontrollResultat)) return false;
        final IdentifiseringsKontrollResultat other = (IdentifiseringsKontrollResultat) o;
        final java.lang.Object this$begrunnelser = this.getBegrunnelser();
        final java.lang.Object other$begrunnelser = other.getBegrunnelser();
        if (this$begrunnelser == null ? other$begrunnelser != null : !this$begrunnelser.equals(other$begrunnelser)) return false;
        return true;
    }

    @java.lang.Override
    @java.lang.SuppressWarnings("all")
    public int hashCode() {
        final int PRIME = 59;
        int result = 1;
        final java.lang.Object $begrunnelser = this.getBegrunnelser();
        result = result * PRIME + ($begrunnelser == null ? 43 : $begrunnelser.hashCode());
        return result;
    }

    @java.lang.Override
    @java.lang.SuppressWarnings("all")
    public java.lang.String toString() {
        return "IdentifiseringsKontrollResultat(begrunnelser=" + this.getBegrunnelser() + ")";
    }
}
