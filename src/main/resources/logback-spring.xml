<configuration>
    <springProfile name="!local">
        <include resource="logback-nais.xml"/>
    </springProfile>
    <springProfile name="local">
        <include resource="logback-local.xml"/>
    </springProfile>
</configuration>